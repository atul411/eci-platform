apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shipping-pvc
  namespace: eci
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: eci
  labels:
    app: shipping-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
    spec:
      containers:
        - name: shipping-service
          image: shipping-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8005
          env:
            - name: PORT
              value: "8005"
            - name: DATABASE_URL
              value: "sqlite:////db/shipping.db"
            - name: SHIPMENTS_CSV
              value: "/data/eci_shipments_indian.csv"
          envFrom:
            - secretRef:
                name: eci-secrets

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 20
            periodSeconds: 30
          volumeMounts:
            - name: db-storage
              mountPath: /db
            - name: data-volume
              mountPath: /data
      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: shipping-pvc
        - name: data-volume
          hostPath:
            path: /eci-data
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: eci
spec:
  selector:
    app: shipping-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service-external
  namespace: eci
spec:
  selector:
    app: shipping-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
      nodePort: 30005
  type: NodePort
